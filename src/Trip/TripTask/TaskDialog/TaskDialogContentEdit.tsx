import type { DialogContentProps } from '../../../Dialog/DialogRoute';
import type { TripSliceTask } from '../../store/types';

export function TaskDialogContentEdit(
  props: DialogContentProps<TripSliceTask>,
) {
    return <div>TODO</div>}
